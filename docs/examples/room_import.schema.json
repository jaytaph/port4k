{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://port4k.com/schema/room-v5.schema.json",
  "title": "Port4k Room v5",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "id", "name", "short", "description", "objects", "exits"],
  "properties": {
    "version": { "const": 5 },
    "id": { "$ref": "#/$defs/Id" },
    "name": { "type": "string", "minLength": 1 },
    "short": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 },
    "kv": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },

    "hints": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "text"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "text": { "type": "string", "minLength": 1 },
          "when": {
            "type": "string",
            "enum": ["enter", "after_fail", "manual", "search", "first_look"]
          },
          "cooldown": { "type": "integer", "minimum": 0 },
          "once": { "type": "boolean" }
        }
      }
    },

    "objects": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/Object" }
    },

    "exits": {
      "type": "array",
      "minItems": 0,
      "items": { "$ref": "#/$defs/Exit" }
    },

    "scripts": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "on_enter": { "$ref": "#/$defs/Lua" },
        "on_command": { "$ref": "#/$defs/Lua" }
      }
    },

    "items_catalog": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "nouns", "short", "description", "stackable"],
        "properties": {
          "id": { "$ref": "#/$defs/Id" },
          "name": { "type": "string", "minLength": 1 },
          "nouns": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          },
          "short": { "type": "string", "minLength": 1 },
          "description": { "type": "string", "minLength": 1 },
          "examine": { "type": "string" },
          "stackable": { "type": "boolean" }
        }
      }
    }
  },

  "$defs": {
    "Id": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_\\-]+$",
      "minLength": 1
    },

    "Direction": {
      "type": "string",
      "enum": [
        "north","south","east","west",
        "northeast","northwest","southeast","southwest",
        "up","down","in","out"
      ]
    },

    "Lua": {
      "type": "string",
      "minLength": 1,
      "description": "Lua chunk; validated as plain string here."
    },

    "Object": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "nouns", "short", "description"],
      "properties": {
        "id": { "$ref": "#/$defs/Id" },
        "nouns": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "short": { "type": "string", "minLength": 1 },
        "description": { "type": "string", "minLength": 1 },

        "flags": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "hidden": { "type": "boolean" },
            "revealed": { "type": "boolean" },
            "takeable": { "type": "boolean" },
            "stackable": { "type": "boolean" }
          }
        },

        "state": {
          "type": "object",
          "additionalProperties": true
        },

        "controls": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^(exit:[a-z]+\\.(locked|visible_when_locked)|object:[A-Za-z0-9_\\-]+\\.[A-Za-z0-9_\\-.]+)$",
            "description": "Lightweight addressing; e.g., 'exit:north.locked'"
          }
        },

        "examine": { "type": "string" },
        "on_use": { "$ref": "#/$defs/Lua" },

        "loot": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "items": {
              "type": "array",
              "items": { "$ref": "#/$defs/Id" }
            },
            "credits": { "type": "integer", "minimum": 0 },
            "once": { "type": "boolean" },
            "shared": {
              "type": "boolean",
              "default": false,
              "description": "false = per-player loot; true = shared loot"
            }
          }
        }
      }
    },

    "Exit": {
      "type": "object",
      "additionalProperties": false,
      "required": ["dir", "to"],
      "properties": {
        "dir": { "$ref": "#/$defs/Direction" },
        "to": { "type": "string", "minLength": 1 },
        "description": { "type": "string" },
        "locked": { "type": "boolean" },
        "visible_when_locked": { "type": "boolean" }
      }
    }
  }
}